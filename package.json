{
  "name": "voicestress-ai-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup:ai": "python -m pip install -r ai-service/requirements.txt",
    "dev": "concurrently \"npm run dev:ai\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:web": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:ai": "python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --app-dir ai-service --reload",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
